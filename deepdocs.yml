
---

### `deepdocs.yml`
```yaml
---
# Minimal DeepDocs config: keep docs and README in sync via PRs
target:
  - url: "docs/"
  - url: "README.md"
include: []
ignore: []
# bump this string to force a full re-scan if needed
reinit: "bootstrap-001"
